#!/bin/sh

_self_get_modules()
{
    cd "$DOE_HOMEDIR/lib" || exit 1
    git clone ${doe_modules_url}
    cd "$DOE_MODDIR" || exit 1
    ln -s ../lib/doe-modules/* .

    echo "${RED}Linked: $($0 list-modules)${NC}"
}

_self_get_modules "$@"
